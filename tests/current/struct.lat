class list {
  int elem;
  list next;
}

int main() {
    list a;
    list b = new list;
     
    //if (a.elem == 0) {
    //    printString("stack");
    //}

    if (b.elem == 0) {
        printString("b equal zero");
    }

    b.elem = 2;

    if (b.elem == 2) {
        printString("now b is equal 2");
    }

    return 0;
}